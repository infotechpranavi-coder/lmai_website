export const metadata = {
    title: 'Events | LMAI - Label Manufacturers Association Of India',
    description: 'Discover upcoming LMAI events, conferences, label awards, and networking opportunities for the Indian label industry.',
};

export default function EventsLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
